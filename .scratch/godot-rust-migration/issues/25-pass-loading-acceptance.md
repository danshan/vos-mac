# 25: 通过完整加载性能和故障验收

**What to build:** 玩家在冻结跨格式工作集上获得符合预算的真实加载体验, 大曲目与失败场景也保持正确.

**Blocked by:** 15: 跨 Chart 复用 VOS 合成缓存; 19: 预热 Chart 并展示完整真实进度; 20: 有界加载音频并回收播放器; 21: 通过 Settings 控制缓存容量; 22: 单实例运行与崩溃后恢复; 24: 冻结跨格式性能与安全验收工作集.

**Status:** ready-for-agent

- [ ] 跨格式验证 warm Gameplay Ready P95 <= 2 s 和 cold P95 <= 5 s; 未达标时定位并修复, 不仅提交失败报告就关闭.
- [ ] cold 从 Chart 选择起包含约 300 ms 等待, 清空该 Chart 与全局 MIDI 派生缓存; 单独记录 OS page cache 条件.
- [ ] 整体成本包含源读取、合成、Rust/Godot 验证和资源加载, 不以 synth 内核时间代替端到端时间.
- [ ] 超过验收规模但未超安全上限的输入继续支持且进度真实, 超安全上限可诊断失败.
- [ ] 累计验证取消、源变化、cache corruption、磁盘不足、崩溃恢复和迟到结果, 不误发布或误启动.
- [ ] 如必须调整架构或预算, 明确提出合同修订并保留阻塞; 不减验证步骤或引入 Java fallback.

